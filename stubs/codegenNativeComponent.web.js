// Web stub for codegenNativeComponent — react-native-web does not implement this.
// Libraries using Fabric native components import this; on web they fall back to
// their own web implementations, so returning null is safe here.
function codegenNativeComponent() {
  return null;
}

module.exports = codegenNativeComponent;
